#include <stdio.h>

int main() {
    int idade = 25;
    float altura = 1.75;
    char genero = 'M';
    char nome[4] = "Joao";
    char letra = 'A';

    // variaveis podem ser declaradas com inicio de letras ou _, mas não pode começar com números

    // int é quando a variável armazena números inteiros
    // float é quando a variável armazena números com casas decimais (valores pequenos)
    // double é quando a variável armazena números com casas decimais (valores grandes)
    // char é quando a variável armazena um caractere (letra, número ou símbolo) ,mas não conseguimos guardar mais de um caractere então para declarar uma palavra usamos um array de char

    // não deixar de inicializar as variáveis para que não entrem lixos de memória
}